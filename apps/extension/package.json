{
  "name": "component-route-explorer",
  "displayName": "Component Route Explorer",
  "description": "Find which app routes use the selected component, then open them in the browser.",
  "version": "0.1.0",
  "publisher": "citadel-minds",
  "license": "MIT",
  "private": false,
  "main": "./dist/extension.js",
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:componentRouteExplorer.showRoutesUsingComponent"
  ],
  "contributes": {
    "commands": [
      {
        "command": "componentRouteExplorer.showRoutesUsingComponent",
        "title": "Show Routes Using This Component"
      }
    ],
    "configuration": {
      "title": "Component Route Explorer",
      "properties": {
        "componentRouteExplorer.baseUrl": {
          "type": "string",
          "default": "http://localhost:3000",
          "description": "Base URL used when opening routes in the browser"
        }
      }
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "package": "npx @vscode/vsce package --no-dependencies --out ../component-route-explorer-0.1.0.vsix"
  },
  "dependencies": {
    "@component-route-explorer/adapter-next-app": "workspace:*",
    "@component-route-explorer/adapter-react-router": "workspace:*",
    "@component-route-explorer/analyzer": "workspace:*",
    "@component-route-explorer/sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "@types/vscode": "^1.95.0",
    "@vscode/vsce": "^3.6.2",
    "typescript": "^5.8.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/citadel-minds/component-route-explorer.git"
  },
  "files": [
    "dist",
    "package.json",
    "README.md",
    "LICENSE"
  ]
}
